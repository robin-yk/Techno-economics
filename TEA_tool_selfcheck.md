# Ethane Cracker TEA Lab — 업로드 전 셀프체크

대상: `ethanecrackertealab.html` (998줄, 단일 파일, 외부 의존성 없음)
대조 자료: `CEJD2527067_R1.pdf` (본문 Methods 4.3 / 2.5절, Fig. 7–8), `060425_Supplementary_Note_2.docx`, `031825_Ethane_Cracking_Tabulated_Results.xlsx`
검증 방법: Chromium 헤드리스 실행(런타임 오류·포커스·반응형·NaN 확인) + 논문 게재값과의 수치 대조

**결론: 이 상태로는 업로드하면 안 됨.** 런타임 에러는 0건이고 UI 완성도는 높지만,
(1) 기본 상태의 결과가 논문의 핵심 결론을 **뒤집고**,
(2) 도구가 "없다"고 선언한 자본비가 **논문 본문에 이미 게재**되어 있으며,
(3) 가장 중요한 필수 입력창(TBMC)이 **타이핑이 안 되는 버그**가 있습니다.

---

## A. 치명적 — 기본 결과가 논문과 반대 결론을 냄

### A-1. 라우트 순위가 역전됨

| | 도구 기본 출력 (COO) | 논문 게재값 (MSP, Fig. 7a) |
|---|---|---|
| Steam cracking | **$751/t** (최고가) | **$680/t** (최저가) |
| MKM-SC₂H₄ | **$567/t** ← 초록색 "best" 표시 | $815–960/t 구간 |
| MKM-XC₂H₆ | $847/t | $815–960/t 구간 |
| Exp-Opt | $695/t | $815–960/t 구간 |

논문의 핵심 주장은 "Joule 가열은 자본비를 낮추지만 전기요금 때문에 MSP는 올라간다"입니다.
현재 도구는 정반대로 **MKM-SC₂H₄를 최저가로 초록 하이라이트**합니다. 심사자/독자가 가장 먼저 보는 화면이 논문을 반박하는 형태입니다.

### A-2. 원인 1 — 벤치마크(steam) 전력비가 약 4배 과대계상

Table S6의 steam 전력 원단위 3.41 kWh/kg에 $0.07/kWh를 곱하면:

```
3.41 kWh/kg × 610,000 t/y × 1000 = 2.080e9 kWh/y × $0.07 = $145.6 M/y
논문 게재값 (2.5절): 산업 벤치마크 전력비 = $37.6 M/y
→ 역산 단가 $0.0181/kWh (비현실적)
```

Joule 경로는 반대로 잘 맞습니다: MKM-SC₂H₄ 3.58 kWh/kg → $152.9 M/y, 논문 $145–154 M/y **거의 일치**.

즉 **Table S6(LCA 인벤토리)의 steam 전력값에는 TEA가 구매비로 계상하지 않은 유틸리티 생산 전력등가가 섞여 있습니다.**
도구의 "한계 3번"에 이 위험이 한 줄 적혀 있지만, 실제로는 경고 수준이 아니라 벤치마크 경로에서 **+$177/t**를 만드는 결정적 오차입니다.
(MKM-XC₂H₆ $196 M/y, Exp-Opt $203 M/y도 논문 $145–154 M/y 범위를 넘습니다 — 4개 경로 모두 재확인 필요)

### A-3. 원인 2 — "자본비는 SI에 없다"는 전제가 사실이 아님

도구는 4개 경로 전부 `TBMC = 0`, "Aspen required", "Capital incomplete"로 표시합니다.
그러나 논문 본문 2.5절에 **TCI가 이미 게재**되어 있습니다:

```
TCI: $317 M (industry) / $242–254 M (MKM 2종) / Exp-Opt ≈ industry 수준
TOC = 2.5 × TBMC  →  TBMC ≈ TCI / 2.5
  steam $126.8 M · MKM-SC₂H₄ $96.8 M · MKM-XC₂H₆ $101.6 M · Exp-Opt $126.8 M
  → 연간 자본비 $24–32 M/y, 유지·보험 $18–24 M/y (합계 $69–91/t)
```
(TCI와 TOC가 정확히 동일하다는 보장은 없으므로 "게재 TCI에서 역산한 기본값, 사용자 수정 가능"으로 명시 필요)

### A-4. 두 가지를 고치면 논문을 재현함 — 이게 가장 중요한 근거

```
steam:  $751/t  −$177/t (전력비 보정)  +$91/t (게재 자본비)  =  $665/t
                                              논문 게재 MSP  =  $680/t   → 오차 2%
```

**이 대조표가 지금 도구 안에 전혀 없습니다.** 검증 레이어를 넣으면 도구는 "재현 불가능한 반쪽짜리"에서
"논문을 2% 이내로 재현하는 도구"로 바뀝니다. 업로드 전 최우선 작업입니다.

### A-5. COO vs MSP 용어 불일치

논문의 대표 지표는 **MSP(minimum selling price)** 이고, 도구가 계산하는 것은 **COO(cost of ethylene)** 입니다.
MSP는 요구수익률을 포함하므로 두 값은 같지 않은데, 도구 어디에도 이 구분이 없습니다.
독자는 $751/t와 $680/t를 같은 축에서 비교하게 됩니다.

---

## B. 게재되어 있는데 도구에 빠진 값

Methods 4.3에 명시된 재무 파라미터가 통째로 누락 — 감사 원장(Assumption ledger)에도 없습니다.

| 게재값 | 출처 | 도구 상태 |
|---|---|---|
| 연간 가동시간 8000 h | Methods 4.3 | **없음** (capacity 610,000 t/y와 정합성 확인 불가. 8000 h × 77 t/h = 616,000 t/y) |
| 세율 35%, 물가 3%, 감가상각 20년 | Methods 4.3 | **없음** |
| 수익이자율 15%, 부채이자율 5% | Methods 4.3 | **없음** (CCF=0.1의 근거인데 미표시) |
| TCM $404 / $504–586 M/y | 2.5절 | **없음** (대조 대상 없음) |
| C₂H₄ 수율 53 / 66 / 56 / 56% | 2.5절 | **없음** (라우트 카드에 전환율만 있음) |
| 순 비에너지 14.7 → 7.4–7.5 / 9.7 GJ/t | 2.5절 | **없음** — 도구의 heat-duty 계산기와 직접 대조 가능한 값인데 미연결 |
| 가열효율 85–95% vs 60% | 2.5절 | 95%로 **상한만** 고정 (범위 아님) |
| 전력원별 MSP: 석탄 $1049, 육상풍력 $694, 해상풍력·태양광·수력 $710–835 | 2.5절, Fig. 8b | **없음** |

---

## C. 사용자 감도(sensitivity) — 현재 수준이 가장 약함

### C-1. 지금 있는 것
에탄가격 1차원 직선 스윕(100–400 $/t) + 전기요금 슬라이더. 끝.
4개 곡선이 전부 **평행한 직선**이라 정보량이 낮습니다.

### C-2. 논문이 명시적으로 요구한 감도항목이 하나도 없음

> "the TEA results carry inherent uncertainty related to **capital costs, operational stability, material lifetime, and scalability**. A sensitivity analysis on these parameters would be valuable in future studies." (2.5절)

- **capital cost** — 슬라이더가 TBMC를 건드리지 않음. TBMC=0이면 감도곡선 전체가 자본비 없는 상태
- **material lifetime (CFP 수명)** — 논문이 지목한 핵심 리스크인데 테스트 불가.
  `cfpPrice`(USD/kg)는 있지만 CFP 원단위가 ~1e-8 kg/kg이라 연간 비용이 **$100 수준**으로 사실상 0.
  필요한 건 "CFP 교체주기(h) → 연간 교체비"이지 kg당 단가가 아님
- **operational stability / scalability** — 가동률(capacity factor), 온스트림 시간 입력 없음

### C-3. 권장 추가
1. **토네이도 차트** — 각 변수 ±X% 시 COO 변동폭. TEA 도구의 사실상 표준
2. **전력원 프리셋 드롭다운** — 석탄/NG/육상풍력/해상풍력/태양광/수력 선택 시 Fig. 8b를 그대로 재현. 게재 그림과 1:1 대조되므로 신뢰도 확보에 가장 효율적
3. **Monte Carlo(선택)** — 논문이 "불확실성 구간 없음"을 자인했으므로 P10/P50/P90 제시 시 부가가치 큼
4. x축 하드코딩 해제 — `sx=x=>L+(x-100)/300*pw`로 100–400 고정. 에탄가격에 800을 입력하면 마커만 400에 클램프되어 **KPI 값과 차트 마커가 서로 다른 값**을 가리킴

---

## D. 기능 버그 (헤드리스 브라우저로 재현 확인 완료)

| # | 심각도 | 내용 |
|---|---|---|
| D-1 | **치명** | **TBMC 입력창에 타이핑 불가.** `#capital-steam`에 "127" 입력 → 값이 `10`이 되고 포커스 소실. 원인: `renderCapitalCards()`의 input 핸들러가 자기 자신을 다시 호출(849행)해 키 입력마다 DOM을 재생성. 도구가 "반드시 입력하라"고 요구하는 **유일한 필수 입력이 동작하지 않음** |
| D-2 | 높음 | **761–1100 px에서 스티키 탭바가 헤더에 가림.** 헤더 버튼이 줄바꿈되며 topbar가 123 px가 되는데 `.tabbar{top:76px}` 고정. 측정: w=900/820/780에서 topbar 123 px vs tabbar top 76 px |
| D-3 | 높음 | **heat 계산기 desync.** Cost inputs에서 전기요금을 0.12로 바꿔도 아래 heat 계산기의 `#heatElec`는 0.07 유지. 가스가격·효율도 동일. 한 화면에 서로 다른 두 값이 공존 |
| D-4 | 중간 | **라우트 카드 효율이 하드코딩.** `jouleEfficiency`를 0.85로 바꿔도 카드는 계속 95% 표시(`routeMeta.efficiency` 정적값). furnace 60%도 동일 |
| D-5 | 중간 | **capacity=0 방어 없음.** 감도차트 path가 `M65 NaN L162.5 NaN…`, 코스트바 width가 빈 문자열이 되어 차트가 조용히 사라짐. 에러 메시지 없음 (`min="0"`이라 입력 가능) |
| D-6 | 중간 | **새로고침하면 전부 소실.** localStorage 미사용. 사용자에게 "Aspen에서 TBMC를 받아 입력하라"고 요구해놓고 그 값을 보존하지 않음 |
| D-7 | 중간 | **Export만 있고 Import 없음.** 게다가 `exportJson()`이 `state.geometry`와 `state.heatDuty`를 누락 → 내보낸 JSON으로 화면을 복원할 수 없음(재현성 목적 미달) |
| D-8 | 낮음 | **CSV에 UTF-8 BOM 없음.** 라우트명에 아래첨자(MKM-SC₂H₄)가 있어 Excel에서 깨짐. `'﻿'` 한 글자로 해결 |

단위 정합성은 전부 통과했습니다 (MJ/kg × t/y = GJ/y 등 냉동·연료·CFP·폐수 모두 확인). 콘솔 에러 0건, 모바일 390 px 가로 오버플로 0.

---

## E. 시각화

### E-1. Cost stack 막대의 구조적 문제
- 막대는 **gross(양의 비용)** 를 그리는데 우측 총계는 **net COO**. 눈으로 읽은 길이와 숫자가 불일치
- 크레딧이 막대 안에 없고 아래 초록 텍스트로만 표기 → 경로 간 크레딧 비교 불가.
  **음수 세그먼트를 왼쪽으로 그리는 diverging 막대**로 바꾸면 해결
- TBMC=0이라 "Annualized capital"(남색)이 폭 0인데 범례에는 남아 있음
- 오른쪽 회색 여백이 "데이터 없음"처럼 읽힘. 축·눈금이 없어 절대값 감각이 없음
- O&M+labor(보라) 세그먼트가 1 px 수준으로 사실상 안 보임

### E-2. 감도차트
- steam `#00539f` vs MKM-SC₂H₄ `#2f7fc1` — **인접한 파랑 2개**. 색각이상·흑백 인쇄에서 구분 불가.
  실선/파선/점선 + 마커 형태를 병행해야 함
- 툴팁이 `<title>` 기반이라 **호버 전용, 키보드 접근 불가**
- SVG에 `role="img"`+`aria-label`은 있으나 데이터 테이블 대체본 없음

### E-3. 레이아웃
- "Feed and power intensity" 패널 하단에 큰 빈 공간(좌측 cost stack과 높이 불일치)
- 감도 패널 좌측 컬럼도 절반이 여백 — 토네이도 차트를 넣을 자리로 적합

### E-4. 접근성
- 탭에 `aria-controls` 없음, 패널에 `aria-labelledby` 없음, 좌우 화살표 키 내비게이션 없음
- `fmt()`가 `toLocaleString(undefined, …)` — 로케일 의존인데 통화기호는 `$` 하드코딩

---

## F. 출처·라벨 문제 (업로드 전 반드시 확인)

### F-1. Supplementary Note 번호 충돌 ⚠️
- 논문 R1 Methods 4.3: **"Details are provided in Supplementary Note 2"** (= TEA 상세)
- 그런데 업로드된 `060425_Supplementary_Note_2.docx`의 제목은 **"Supplementary Note 2. Transport and Kinetic Analysis"** — 경제 내용이 전혀 없음(전문 검색으로 capital/labor/USD/Seider 등 0건)
- 도구는 경제 가정 15개를 전부 **"SI Note 2, assumption 1–15"** 로 인용

→ 초안 간 노트 번호가 어긋나 있습니다. 이대로 올리면 감사 원장의 **모든 인용이 잘못된 노트를 가리킵니다.** 최종 SI 번호로 일괄 수정 필요.

### F-2. 전환율 라벨
논문 2.5절은 시나리오를 **MKM-SC₂H₄ = 선택도 80%**, **MKM-XC₂H₆ = 전환율 ~65%** 로 정의합니다.
도구는 `mkmS: conversion 80.2%`, `exp: conversion 80.0%`로 **둘 다 "Conversion"** 라벨을 붙였습니다.
`mkmX: 64.7%`는 논문 "~65%"와 일치하니 문제없지만, mkmS/exp는 Table S4 원본으로 재확인해야 합니다.
(라우트 카드에 논문 게재 **수율** 53/66/56/56%를 함께 넣으면 오해가 사라집니다)

### F-3. 설비용량 근거
"600,000–620,000 t/y의 중간값 610,000"이라 적혀 있으나 본문에서 이 범위를 찾지 못했습니다.
Methods의 8000 h/y와 조합하면 616,000 t/y가 나옵니다. SI 원문 쪽수를 명시하거나 근거를 재확인해 주세요.

---

## G. 업로드 전 작업 순서 (권장)

**반드시 (이거 없이는 업로드 불가)**
1. D-1 TBMC 입력 포커스 버그 — `renderCapitalCards()` 재귀 호출 제거, 미니지표만 부분 갱신
2. A-3 게재 TCI에서 역산한 TBMC를 기본값으로 프리필 + "게재 TCI 역산" 태그
3. A-2 벤치마크 전력 처리 수정 — Table S6 전력을 그대로 곱하지 말고, 게재 전력비($37.6 M/y)와의 괴리를 화면에 명시
4. A-4 **검증 패널 신설** — TCI/TCM/MSP/전력비 계산값 vs 게재값 대조표(차이 % 포함)
5. A-5 COO와 MSP 구분 문구 명시
6. F-1 SI 노트 번호 정정

**강력 권장**
7. 토네이도 차트 + 전력원 프리셋(Fig. 8b 재현)
8. CFP 교체주기(h) 입력 → 연간 교체비
9. 8000 h/y·세율·이자율 등 게재 재무 파라미터를 입력·감사 원장에 추가
10. D-2 탭바 오버랩, D-3 heat 계산기 desync, D-4 효율 하드코딩, D-5 capacity=0 방어

**여유 되면**
11. localStorage 저장 + JSON Import
12. Cost stack을 diverging 막대로, 감도차트에 파선/마커 추가
13. CSV BOM, 접근성(aria-controls, 화살표 키, 데이터 테이블 대체본)
