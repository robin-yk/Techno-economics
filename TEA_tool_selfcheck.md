# Ethane Cracker TEA Lab — 업로드 전 셀프체크

대상: `ethanecrackertealab.html` (998줄, 단일 파일, 외부 의존성 없음)

대조 자료:
- `SI_UD_MittalKwak_2025_...pdf` — **실제 SI (Supplementary Note 2 = TEA, Note 3 = LCA, Table S4–S7)**
- `CEJD2527067_R1.pdf` — 본문 Methods 4.3 / 2.5절, Fig. 7–8
- `060425_Supplementary_Note_2.docx` — 별도 초안(Transport & Kinetic Analysis, 번호 충돌)
- `031825_Ethane_Cracking_Tabulated_Results.xlsx`

검증 방법: Chromium 헤드리스 실행(런타임 오류·포커스·반응형·NaN) + SI 원문 값 전수 대조 + 논문 게재 결과값과의 수치 대조

**결론: 데이터 전사는 완벽합니다. 하지만 기본 상태의 결과가 논문 결론을 뒤집고, 필수 입력창이 동작하지 않아 원본은 업로드 불가입니다.**

> **수정 완료 — `ethane-cracker-tea-lab.html`**
> 아래 A~F에서 지적한 항목 중 G절의 "반드시" + "강력 권장" 전부를 반영한 수정본을 같은 리포에 커밋했습니다.
> 검증 결과는 이 문서 맨 아래 [H. 수정 결과](#h-수정-결과-검증-완료) 참조.
> 아래 A~F 본문은 **원본 도구에 대한 감사 기록**이며, 수정본에서 해소된 항목은 H절에 대응표로 정리했습니다.

---

## 0. 먼저 — SI 전수 대조 결과 (전부 통과)

SI를 받고 나서 값을 하나씩 대조한 결과, **전사 오류가 0건**입니다. 이 부분은 신뢰해도 됩니다.

| 항목 | 결과 |
|---|---|
| Table S6 인벤토리 **56개 값 전부** (4경로 × 14항목) | ✅ 완전 일치 (1.6E-05, 9.2E-9, 6.9E-8 같은 지수값 포함) |
| Table S4 반응식 3종 + 전환율 (80.2 / 64.7 / 80%) | ✅ 완전 일치. Exp-Opt 좌변 계수 `0.99C₂H₆`까지 정확 |
| Table S5 운전조건 **24행 전부** (T·P·환류비) | ✅ 완전 일치 (2.715, 0.5636, 3.33, 1.95, 16 등) |
| Eq. S8 / S9 구조, TOC=2.5×TBMC, CCF=0.1 | ✅ 일치 |
| 경제 가정 인용번호 (assumption 1,2,3,4,5,8,9,10,11,15) | ✅ SI 실제 번호와 전부 일치 |
| 144 tubes / D=0.12 m (가정 6) | ✅ 일치. 튜브길이를 유량·체류시간에서 역산하는 계산기 설계도 가정 6의 서술과 정확히 부합 |
| φ(volume factor) 기본값 1 | ✅ 가정 7 "CFP completely occupies the tube"와 부합 |
| 용량 610,000 t/y = 600,000–620,000 중간값 | ✅ 가정 1에 명시. SI 3.1절 "~77 t/h" × 8000 h = 616,000 t/y로 교차 검증됨 |
| 노 60% / Joule 95% 효율 | ✅ 가정 15와 일치 |
| "Seider 유틸리티 단가는 수치 미제공" | ✅ 사실 (가정 14는 인용만) |
| "TBMC는 SI에 없음" | ✅ 사실 (SI 3.2절은 Aspen Economic Analyzer 사용만 언급) |

### 이전 리포트에서 제가 틀렸던 항목 — 철회합니다

SI 없이 본문만으로 판단해 잘못 지적했던 3건입니다. **도구가 맞고 제가 틀렸습니다.**

1. ~~"전환율 라벨이 선택도와 혼동됨"~~ → Table S4는 명시적으로 $X_{C_2H_6}$ = 80.2 / 64.7 / 80%. 도구가 정확합니다.
2. ~~"SI Note 번호 충돌"~~ → 실제 SI에서 **Supplementary Note 2 = TEA**가 맞습니다. 도구의 인용이 전부 맞습니다. 다만 별도 업로드된 `060425_Supplementary_Note_2.docx`는 같은 번호를 쓰는 **다른 초안**(Transport & Kinetic Analysis)이니, 최종 SI 패키지에서 번호가 겹치지 않는지만 확인하면 됩니다. **도구는 수정할 것이 없습니다.**
3. ~~"효율 85–95% 범위인데 95% 상한 고정"~~ → SI 가정 15는 **95% 단일값**입니다. 본문의 "85–95%"는 별개 서술이고, TEA 기준값은 95%가 맞습니다.

---

## A. 치명적 — 기본 결과가 논문과 반대 결론을 냄

### A-1. 라우트 순위가 역전됨

| | 도구 기본 출력 (COO) | 논문 게재값 (Fig. 7a) |
|---|---|---|
| Steam cracking | **$751/t** (최고가) | **$680/t** (최저가) |
| MKM-SC₂H₄ | **$567/t** ← 초록색 "best" | $815–960/t 구간 |
| MKM-XC₂H₆ | $847/t | $815–960/t 구간 |
| Exp-Opt | $695/t | $815–960/t 구간 |

논문의 핵심 주장은 "Joule 가열은 자본비를 낮추지만 전기요금 때문에 판매가는 올라간다"입니다. 도구는 정반대로 MKM-SC₂H₄를 최저가로 초록 하이라이트합니다. 첫 화면이 논문을 반박하는 형태입니다.

### A-2. 원인 1 — Table S6는 TEA가 아니라 **LCA** 인벤토리

SI 구조를 보면 명확합니다.

- **Note 2 (TEA)**: 비용은 Aspen Economic Analyzer 출력 + Seider 유틸리티 단가. 인벤토리 표 없음.
- **Note 3 (LCA)**: Table S6 = "Inputs and outputs associated with the production of 1 kg of C₂H₄" — **LCIA용**.

SI 어디에도 Table S6 흐름이 TEA OPEX에 그대로 들어갔다는 서술이 없습니다. 도구는 서로 다른 두 Note를 가교해 OPEX를 재구성하는데, 벤치마크 경로에서 그 가교가 무너집니다:

```
3.41 kWh/kg × 610,000 t/y × 1000 = 2.080e9 kWh/y × $0.07 = $145.6 M/y
논문 게재값 (2.5절): 산업 벤치마크 전력비 = $37.6 M/y
→ 역산 단가 $0.0181/kWh (비현실적)
```

Joule 경로는 반대로 잘 맞습니다 — MKM-SC₂H₄ 3.58 kWh/kg → $152.9 M/y vs 논문 $145–154 M/y **거의 일치**. 즉 LCA의 steam 전력값에는 TEA가 전력비로 계상하지 않은 스팀·냉동 유틸리티의 전력등가가 섞여 있습니다. 벤치마크에만 **+$177/t**가 얹힙니다.

(MKM-XC₂H₆ $196 M/y, Exp-Opt $203 M/y도 논문 $145–154 M/y 범위를 넘습니다)

도구의 "한계 3번"에 이 위험이 한 줄 있지만, 경고 수준이 아니라 결론을 뒤집는 오차입니다.

### A-3. 원인 2 — "자본비는 SI에 없다"는 맞지만, **논문 본문에는 있습니다**

도구는 4경로 전부 `TBMC = 0`, "Aspen required", "Capital incomplete"로 둡니다. SI에 TBMC가 없다는 진단은 정확합니다. 그러나 논문 본문 2.5절에 **TCI가 게재**되어 있어 역산이 가능합니다:

```
TCI: $317 M (industry) / $242–254 M (MKM 2종) / Exp-Opt ≈ industry 수준
TOC = 2.5 × TBMC (가정 11)  →  TBMC ≈ TCI / 2.5
  steam $126.8 M · MKM-SC₂H₄ $96.8 M · MKM-XC₂H₆ $101.6 M · Exp-Opt $126.8 M
  → 연간 자본비 $24–32 M/y, 유지·보험 $18–24 M/y (합계 $69–91/t)
```

TCI와 TOC가 정확히 같다는 보장은 없으므로 "게재 TCI에서 역산한 기본값, 수정 가능"으로 태깅하면 됩니다.

### A-4. 두 가지를 고치면 논문을 재현합니다 — 가장 중요한 근거

```
steam:  $751/t  −$177/t (전력비 보정)  +$91/t (게재 자본비)  =  $665/t
                                              논문 게재값     =  $680/t   → 오차 2%
```

**이 대조표가 지금 도구에 전혀 없습니다.** 검증 패널을 넣으면 "재현 불가능한 반쪽짜리"에서 "논문을 2% 이내로 재현하는 도구"로 바뀝니다. 최우선 작업입니다.

### A-5. HP 스팀 10.7 kg/kg 단일 값이 MKM-XC₂H₆ 순위를 혼자 결정함

Table S6 전사는 정확하지만(10.7 맞음), 이 값이 이례적입니다:

| 경로 | HP steam | $25/t 기준 비용 |
|---|---|---|
| steam | 3.78 | $57.6 M/y |
| MKM-SC₂H₄ | 2.03 | $31.0 M/y |
| **MKM-XC₂H₆** | **10.70** | **$163.2 M/y** ← gross OPEX의 **29%** |
| Exp-Opt | 2.35 | $35.8 M/y |

스팀 희석이 가장 높은 쪽은 MKM-SC₂H₄(40 wt%)인데 HP 스팀은 오히려 최저입니다. MKM-XC₂H₆만 5배입니다. 이 값이 2.4 수준이었다면 COO가 **$847 → $640/t**로 순위가 다시 뒤집힙니다.

전사 오류는 아니므로 도구 잘못은 아니지만, 결과가 이 한 값에 매달려 있으므로 (a) 저자 확인, (b) 도구 내 "이 값이 결과를 지배함" 경고 표시를 권합니다.

### A-6. COO vs MSP — SI 자체가 두 용어를 혼용

SI 가정 5는 "**minimum selling price** of C₂H₄를 부산물 크레딧 가정 하에 계산", 가정 13은 "**Cost of C₂H₄ (COO)** = Eq. S8"이라 씁니다. 별도 MSP 산식은 SI에 없습니다.

즉 이 논문에서는 사실상 COO ≡ MSP로 쓰이고 있어, 도구가 Eq. S8로 COO를 계산해 $680/t와 비교하는 것은 **정당합니다**. (이전 리포트에서 이 점을 과하게 지적했는데 완화합니다.) 다만 원문이 혼용하고 있다는 사실을 도구 각주로 한 줄 남기면 독자 혼란이 없습니다.

---

## B. 신규 치명 — SI 가정 7(CFP 월 1회 교체)이 비용모델에 없음

이전 리포트에서 "CFP 수명을 테스트할 수 없다"고 적었는데, SI를 보니 더 심각합니다. **SI가 교체주기를 명시하고 있는데 도구가 이를 구현하지 않았습니다.**

> **가정 7.** "To attain near ideal temperature profile for MKM, we assume CFP completely occupies the tube. Despite relatively long time on stream performance, **we assume monthly replacement.**"

가정 6·7 + 본문의 CFP 사양(Freudenberg H23, 0.21 mm, 95 g/m²)으로 직접 계산됩니다:

```
CFP 벌크밀도 = 0.095 kg/m² ÷ 0.00021 m = 452 kg/m³
반응기 체적 (도구 기본 형상: 1000 kmol/h, 1000 °C, 1 bar, 100 ms)
  Q = 29.4 m³/s → V = 2.94 m³ → L = 1.81 m (144 tubes, D=0.12 m)
CFP 장입량 = 2.94 m³ × 452 = 1,330 kg
월 1회 교체 → 15,961 kg/y ≈ 16 t/y
  $20/kg  → $0.32 M/y = $0.52/t C₂H₄
  $50/kg  → $0.80 M/y = $1.31/t
  $100/kg → $1.60 M/y = $2.62/t
```

**도구가 현재 계산하는 값: 연 $112.**
(Table S6의 CFP 9.2E-9 kg/kg × 610,000 t/y × $20/kg — 같은 단가 기준으로 **2,844배 과소**)

Table S6의 CFP 값은 LCA용 질량배분 수치이지 TEA의 소모량이 아닙니다. 도구는 CFP 비용을 사실상 0으로 모델링하고 있어서, 논문이 스스로 지목한 핵심 리스크(material lifetime)가 화면에서 완전히 사라집니다.

**필요한 것**: `cfpPrice`(USD/kg)는 유지하되, 입력을 "교체주기(개월)"로 바꾸고 반응기 체적 × 밀도 × 연간 교체횟수로 계산. 그러면 형상 계산기(가정 6)와 비용모델(가정 7)이 연결되면서 지금 고립돼 있는 튜브길이 계산기가 비로소 쓸모가 생깁니다.

---

## C. 사용자 감도 — 논문이 요구한 항목이 없음

### C-1. 지금 있는 것
에탄가격 1차원 직선 스윕(100–400 $/t) + 전기요금 슬라이더. 곡선 4개가 평행 직선이라 정보량이 낮습니다.

### C-2. 논문이 명시적으로 요구한 감도항목

> "the TEA results carry inherent uncertainty related to **capital costs, operational stability, material lifetime, and scalability**. A sensitivity analysis on these parameters would be valuable in future studies." (본문 2.5절)

- **capital cost** — 슬라이더가 TBMC를 건드리지 않음. TBMC=0이면 감도곡선 전체가 자본비 없는 상태
- **material lifetime** — B절 참조. SI 가정 7에 "월 1회"라는 기준선이 있으므로 **교체주기 슬라이더로 즉시 구현 가능**
- **operational stability / scalability** — 가동률·온스트림 시간 입력 없음 (8000 h/y도 미노출)

### C-3. 권장 추가
1. **토네이도 차트** — TEA 도구 사실상 표준. 감도 패널 좌측 여백에 그대로 들어감
2. **전력원 프리셋** — 석탄 $1049 / 육상풍력 $694 / 해상풍력·태양광·수력 $710–835 (본문 2.5절, Fig. 8b). 게재 그림과 1:1 대조되므로 신뢰도 확보에 가장 효율적
3. **CFP 교체주기 슬라이더** (1–12개월)
4. x축 하드코딩 해제 — `sx=x=>L+(x-100)/300*pw`로 100–400 고정. 에탄가격에 800을 넣으면 마커만 400에 클램프돼 **KPI 값과 차트 마커가 다른 값**을 가리킴
5. Monte Carlo (선택) — 논문이 "불확실성 구간 없음"을 자인했으므로 P10/P50/P90 제시 시 부가가치 큼

---

## D. 기능 버그 (헤드리스 브라우저 재현 확인 완료)

| # | 심각도 | 내용 |
|---|---|---|
| D-1 | **치명** | **TBMC 입력창에 타이핑 불가.** `#capital-steam`에 "127" 입력 → 값이 `10`이 되고 포커스 소실. 원인: `renderCapitalCards()`의 input 핸들러가 자기 자신을 재호출(849행)해 키 입력마다 DOM 재생성. 도구가 "반드시 입력하라"고 요구하는 **유일한 필수 입력이 동작하지 않음** |
| D-2 | 높음 | **761–1100 px에서 스티키 탭바가 헤더에 가림.** 헤더 버튼 줄바꿈으로 topbar가 123 px가 되는데 `.tabbar{top:76px}` 고정. 측정: w=900/820/780에서 topbar 123 px vs tabbar top 76 px |
| D-3 | 높음 | **heat 계산기 desync.** Cost inputs에서 전기요금을 0.12로 바꿔도 아래 heat 계산기 `#heatElec`는 0.07 유지. 가스가격·효율도 동일. 한 화면에 두 개의 진실 |
| D-4 | 중간 | **라우트 카드 효율 하드코딩.** `jouleEfficiency`를 0.85로 바꿔도 카드는 95% 유지(`routeMeta.efficiency` 정적값). furnace 60%도 동일 |
| D-5 | 중간 | **capacity=0 방어 없음.** 감도차트 path가 `M65 NaN L162.5 NaN…`, 코스트바 width가 빈 문자열이 되어 차트가 조용히 사라짐. 에러 메시지 없음 (`min="0"`이라 입력 가능) |
| D-6 | 중간 | **새로고침 시 전부 소실.** localStorage 미사용. "Aspen에서 TBMC를 받아 입력하라"고 요구해놓고 그 값을 보존하지 않음 |
| D-7 | 중간 | **Export만 있고 Import 없음.** `exportJson()`이 `state.geometry`·`state.heatDuty`를 누락 → 내보낸 JSON으로 화면 복원 불가(재현성 목적 미달) |
| D-8 | 낮음 | **CSV에 UTF-8 BOM 없음.** 라우트명 아래첨자(MKM-SC₂H₄)가 Excel에서 깨짐 |

단위 정합성 전수 통과 (MJ/kg × t/y = GJ/y, 냉동·연료·CFP·폐수·스팀 모두 확인). 콘솔 에러 0건, 모바일 390 px 가로 오버플로 0.

---

## E. 시각화

### E-1. Cost stack 막대
- 막대는 **gross**를 그리는데 우측 총계는 **net COO** → 길이와 숫자가 불일치
- 크레딧이 막대 밖 초록 텍스트로만 있어 경로 간 비교 불가. **음수 세그먼트를 왼쪽으로 그리는 diverging 막대**로 해결
- TBMC=0이라 "Annualized capital"(남색) 폭이 0인데 범례에는 남아 있음
- 우측 회색 여백이 "데이터 없음"처럼 읽힘. 축·눈금이 없어 절대값 감각 없음
- O&M+labor(보라) 세그먼트가 1 px 수준으로 사실상 안 보임

### E-2. 감도차트
- steam `#00539f` vs MKM-SC₂H₄ `#2f7fc1` — **인접 파랑 2개**. 색각이상·흑백 인쇄에서 구분 불가. 실선/파선/점선 + 마커 병행 필요
- 툴팁이 `<title>` 기반이라 **호버 전용, 키보드 접근 불가**
- `role="img"`+`aria-label`은 있으나 데이터 테이블 대체본 없음

### E-3. 레이아웃·접근성
- "Feed and power intensity" 패널 하단 큰 빈 공간(좌측과 높이 불일치)
- 감도 패널 좌측 절반이 여백 — 토네이도 차트 자리로 적합
- 탭에 `aria-controls` 없음, 패널에 `aria-labelledby` 없음, 좌우 화살표 키 내비게이션 없음
- `fmt()`가 `toLocaleString(undefined, …)` 로케일 의존인데 통화기호는 `$` 하드코딩

---

## F. SI에는 있는데 도구에 없는 값

| 게재값 | 출처 | 상태 |
|---|---|---|
| **CFP 월 1회 교체** | SI 가정 7 | **없음** — B절, 최우선 |
| 연간 가동 8000 h | 본문 Methods 4.3 | 없음 (610,000 t/y와의 정합 근거) |
| 세율 35%, 물가 3%, 감가상각 20년, 수익이자율 15%, 부채이자율 5% | 본문 Methods 4.3 | 없음 (CCF=0.1의 근거) |
| 열교환기 10–17기 | SI 3.2절 | 없음 |
| TCM $404 / $504–586 M/y | 본문 2.5절 | 없음 (대조 대상 없음) |
| C₂H₄ 수율 53 / 66 / 56 / 56% | 본문 2.5절 | 없음 (라우트 카드에 전환율만) |
| 순 비에너지 14.7 → 7.4–7.5 / 9.7 GJ/t | 본문 2.5절 | 없음 — 도구의 heat-duty 계산기와 직접 대조 가능한 값인데 미연결 |
| 전력원별 값: 석탄 $1049, 육상풍력 $694, 해상풍력·태양광·수력 $710–835 | 본문 2.5절 / Fig. 8b | 없음 |
| 생산량 ~77 t/h | SI 3.1절 | 없음 (용량 입력의 교차검증에 유용) |

---

## G. 업로드 전 작업 순서

**반드시**
1. D-1 TBMC 입력 포커스 버그 — `renderCapitalCards()` 재귀 호출 제거, 미니지표만 부분 갱신
2. B절 CFP 교체주기 구현 (SI 가정 7) — 형상 계산기와 비용모델 연결
3. A-3 게재 TCI 역산 TBMC를 기본값으로 프리필 + "게재 TCI 역산" 태그
4. A-2 Table S6가 **LCA** 인벤토리임을 전면에 명시하고, 게재 전력비($37.6 M/y)와의 괴리를 화면에 표시
5. A-4 **검증 패널 신설** — TCI/TCM/전력비/최종가 계산값 vs 게재값 대조표(차이 % 포함)
6. A-5 MKM-XC₂H₆ HP 스팀 10.7 kg/kg 저자 확인 + 지배변수 경고

**강력 권장**
7. 토네이도 차트 + 전력원 프리셋(Fig. 8b 재현) + CFP 교체주기 슬라이더
8. 8000 h/y·세율·이자율 등 게재 재무 파라미터를 입력·감사 원장에 추가
9. D-2 탭바 오버랩, D-3 heat 계산기 desync, D-4 효율 하드코딩, D-5 capacity=0 방어
10. A-6 COO/MSP 용어 각주

**여유 되면**
11. localStorage 저장 + JSON Import
12. Cost stack diverging 막대, 감도차트 파선/마커
13. CSV BOM, 접근성(aria-controls, 화살표 키, 데이터 테이블 대체본)

**수정 불필요 (확인 완료)**
- Table S4/S5/S6 전사값, 인용번호, 용량 근거, 효율 95%, 144튜브/0.12 m, Eq. S8/S9 구조

---

## H. 수정 결과 (검증 완료)

수정본: **`ethane-cracker-tea-lab.html`** — 여전히 단일 파일, 외부 라이브러리·네트워크 호출 없음.
검증: Chromium 헤드리스에서 콘솔/페이지 에러 0건, 6개 탭 전부 정상 렌더, 모바일 390 px 전 탭 가로 오버플로 0, 인쇄 미디어에서 6개 패널 모두 출력, CSV/JSON export 정상.

### H-1. 핵심 성과 — 벤치마크를 2.2% 오차로 재현

Validation 탭에서 전력 기준을 **"Published TEA electricity cost"** 로 전환하면:

| 경로 | TOC vs 게재 TCI | TCM vs 게재 | 전력비 vs 게재 | **최종가 vs 게재** |
|---|---|---|---|---|
| Steam cracking | 317.0 / $317 **0.0%** | 373.9 / $404 −7.4% | 37.6 / $37.6 **0.0%** | **$665 / $680 −2.2%** |
| MKM-SC₂H₄ | 242.0 / $242–254 **0.0%** | 361.0 / $504–586 −28.4% | 149.5 / $145–154 **0.0%** | $632 / $815–960 −22.5% |
| MKM-XC₂H₆ | 254.0 / $242–254 **0.0%** | 489.5 / $504–586 −2.9% | 149.5 / $145–154 **0.0%** | **$844 / $815–960 범위 내** |
| Exp-Opt | 317.0 / $317 **0.0%** | 394.7 / $504–586 −21.7% | 149.5 / $145–154 **0.0%** | $699 / $815–960 −14.2% |

벤치마크와 MKM-XC₂H₆가 맞고 나머지 두 경로가 −14~−22%로 낮게 나옵니다. 사료·크레딧·자본·인건비는 전부 게재값이므로 잔차는 **Seider 유틸리티 단가**에 있습니다 — 도구가 이 진단을 자동으로 표시합니다.

### H-2. 지적 항목 대응표

| 항목 | 수정 내용 | 검증 |
|---|---|---|
| A-1/A-2 LCA·TEA 경계 | Validation 탭 신설 + 전력 기준 토글(Table S6 인벤토리 ↔ 게재 TEA 전력비). Table S6가 **Note 3의 LCA 인벤토리**임을 인벤토리 표·한계·감사원장에 명시 | 인벤토리 기준에서 steam 전력비 +287% 편차를 자동 경고, 필요 단가 $0.0181/kWh를 명시 |
| A-3 자본비 | 게재 TCI ÷ 2.5로 TBMC 프리필 (126.8 / 96.8 / 101.6 / 126.8 M) + "From TCI" 태그 | TOC가 4경로 모두 게재 TCI와 0.0% 일치 |
| A-4 검증 패널 | TOC·TCM·전력비·최종가 4개 지표 × 4경로 대조표, 편차 %와 색상 플래그(±10/25%) | 위 H-1 표 |
| A-5 HP 스팀 | 인벤토리 **이상치 탐지**로 전환 — 타 경로 중앙값 3배 초과 + OPEX 15% 초과일 때만 경고 | "MKM-XC₂H₆: H.P. steam is a 3.7× outlier … 29% of gross OPEX" 정확히 1건만 발화 (전기는 설계상 차이라 제외) |
| A-6 COO/MSP | 한계 7번에 SI가 두 용어를 혼용한다는 사실 명시 | — |
| **B. CFP 가정 7** | Table S6 배분값 폐기. `V_reactor × φ × ρ × (12/교체주기)` 로 계산, 전용 패널 + 교체주기 입력 신설. Joule 경로만 부과 | 장입 **1,329 kg**, 연 **15.9 t/y** (수동 계산과 일치). Table S6 방식이면 연 $112라는 대비를 화면에 표시 |
| C-1~3 감도 | **토네이도 차트** 신설(13개 변수, ±% 조절), CFP 교체주기·TBMC·CCF 포함. Fig. 8b 전력원별 게재값 → 필요 $/kWh 역산표 | Joule 경로 토네이도에 CFP 2개 항목 정상 노출. 석탄 $0.1146/kWh, 육상풍력 $0.0373/kWh 역산 |
| C-4 x축 하드코딩 | 마커 위치에 따라 x범위 자동 확장 | 마커가 항상 곡선 위에 위치 |
| D-1 TBMC 포커스 | 핸들러의 자기 재렌더 제거, 미니지표만 부분 갱신 | "127" 입력 → 값 `127`, 포커스 유지 ✅ |
| D-2 탭바 오버랩 | `--topbar-h`를 ResizeObserver로 실측 동기화 | 7개 폭 전부 topbar 높이 = tabbar top 일치 |
| D-3 heat desync | 양방향 동기화(포커스된 입력은 건드리지 않음) | 0.12 → heatElec 0.12, heatGas 9 → screening 9 ✅ |
| D-4 효율 하드코딩 | 라우트 카드가 state를 읽음 | jouleEff 0.85 → 카드 `60% 85% 85% 85%` ✅ |
| D-5 capacity=0 | 3개 차트 전부 가드 + 안내 문구 | NaN path 0건, 에러 0건 |
| D-6 영속성 | localStorage 저장/복원 | 새로고침 후 입력값·기준 유지 ✅ |
| D-7 Import | JSON 불러오기 추가, geometry·heatDuty 포함 | 라운드트립 확인 |
| D-8 CSV | UTF-8 BOM + CRLF, 게재값·편차·CFP 열 추가 | BOM 확인 |
| E-1 Cost stack | **diverging 막대** — 크레딧을 0선 왼쪽에 **동일 스케일**로, 축 눈금과 net 캐럿 추가 | 길이 비교가 실제 금액비와 일치 |
| E-2 감도차트 | 경로별 파선 패턴 + 마커 형태(원/사각/삼각/마름모) | 흑백·색각이상에서도 구분 가능 |
| E-4 접근성 | `aria-controls`/`aria-labelledby`, 좌우·Home/End 키 내비게이션, `:focus-visible` | — |
| F. 누락 게재값 | 가동시간 8000 h/y(+용량 정합성 자동 체크), 재무 파라미터, 수율, 순 비에너지, TCI/TCM/전력비, ~77 t/h 추가 | 610,000 t/y ÷ 8000 h = 76.3 t/h ✓ |

### H-3. 남은 판단 사항 (도구로 해결 불가)

1. **MKM-SC₂H₄ / Exp-Opt의 −14~−22% 편차** — Seider 유틸리티 단가 원본이 필요합니다. 도구가 편차를 표시하므로 실제 단가를 넣으면 즉시 수렴 여부를 확인할 수 있습니다.
2. **MKM-XC₂H₆ HP 스팀 10.7 kg/kg** — 전사는 정확하나 타 경로 대비 3.7배입니다. 저자 확인이 필요합니다.
3. **TCI ≈ TOC 가정** — TCI에 운전자본이 포함되면 TBMC 역산이 과대평가됩니다. 한계 3번에 명시했습니다.
4. **CFP 반응기 체적** — 가정 6·7은 교체주기와 튜브 제원을 주지만 유량·체류시간은 SI에 없어 사용자 입력으로 남습니다.
